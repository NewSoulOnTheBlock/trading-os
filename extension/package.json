{
  "name": "trading-os-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Trading OS — injects realized-P&L coaching, risk circuit breaker and analytics directly into axiom.trade",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
